name: smart_shift_optimizer
description: Smart Shift Optimizer Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  open_file: ^3.3.2
  flutter:
    sdk: flutter
  fl_chart: ^0.65.0
  flutter_localizations:
    sdk: flutter

  provider: ^6.0.5
  shared_preferences: ^2.2.0
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path_provider: ^2.1.1
  path: ^1.8.3
  file_picker: ^8.1.2
  local_auth: ^2.1.7
  crypto: ^3.0.3
  url_launcher: ^6.2.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/translations/